version: '3'

services:
  app:
    build: .
    image: mattipaksula/hitbox
    #environment:
    # - REDIS_URL=redis://redis:6379/0
    ports:
      - 8080:8080
  redis:
    image: redis:alpine
    ports:
      - 6379:6379
